<!DOCTYPE html>
<html>

    <head>
        <meta charset="UTF-8">
        <title>
            {% block title %}Welcome!
            {% endblock %}
        </title>
         <link rel="stylesheet" href="https://bootswatch.com/4/united/bootstrap.min.css">
         <link rel="stylesheet" href="{{asset('css/style.css')}}"> 
      <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/js/bootstrap.min.js"></script>
         
    
        {% block stylesheets %}{% endblock %}
        </head>

        <body>
            <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow p-3 mb-5">
                <a class="navbar-brand" href="#">MARMITTON</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav mr-auto">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{path ('recipe_index')}}">Acceuil
                                <span class="sr-only">(current)</span>
                            </a>
                        </li>


                        {% if not app.user %}


                            <li class="nav-item">
                                <a class="nav-link" href="{{path ('app_registration')}}">S'enregistrer /</a>
                            </li>
                            <li class="nav-item">

                                <a class="nav-link" href="{{path ('app_login')}}">/ Se connecter</a>
                            </li>
                        {% else %}

                            <li class="nav-item">
                                <a class="nav-link" href="{{path ('recipe_new')}}">Ajouter une recette</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{path ('app_logout')}}"> | Se deconnecter</a>
                            </li>
                        {% endif %}


                    </ul>
                </div>
                {% if app.user %}
                    <div class="text-white text-right text-uppercase">
                        Bonjour ,
                        <img src="http://www.icône.com/images/icones/2/0/button-green.png" alt="photo_de_logging" class="icon">
                    <strong>    {{app.user.name}} </strong>
                        !
                    </div>
                {% endif %}
            </nav>
   {% block body %}{% endblock %}
                

                {% block javascripts %}{% endblock %}
            </body>

        </html>
